/*Crear una función que muestre el contenido de un array,
 cada elemento en una línea distinta
*/
let miArrayNormal = [1, 6, 8, 54, 3, 2, 6, 7, 10, 9, 51,50];
let mostrar = 0;
for(i=0;i<=miArrayNormal.length;i++){
  mostrar = miArrayNormal[i];
  console.log(mostrar);
}
